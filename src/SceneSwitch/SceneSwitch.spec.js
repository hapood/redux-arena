/* eslint-env mocha */
import React from "react";
import { shallow } from "enzyme";
import { expect, assert } from "chai";
import sinon from "sinon";
import SceneSwitch from "./SceneSwitch";

describe("<SceneSwitch />", () => {
  // it("set history when ReduxArena is mounted", () => {
  //   const setArenaHistorySpy = sinon.spy();
  //   const wrapper = shallow(
  //     <ReduxArena setArenaHistory={setArenaHistorySpy} />
  //   );
  //   expect(setArenaHistorySpy).to.have.property("callCount", 1);
  //   assert.notEqual(setArenaHistorySpy.args[0].push, null);
  // });
});
